# hackday-ai-helloworld

Sample login UI built with TypeScript.

## Run locally in VS Code

1. Open this repository in Visual Studio Code.
2. Install dependencies:
   ```bash
   npm install
   ```
3. Build TypeScript:
   ```bash
   npm run build
   ```
4. Open `public/index.html` in a browser.
